@extends('admin.reports.base') @section('report_title','Laporan Pesanan') @section('report_content')<table><thead><tr><th>No. Pesanan</th><th>Pelanggan</th><th>Produk</th><th>Total</th><th>Status</th><th>Tanggal</th></tr></thead><tbody>@foreach($orders as $order)<tr><td>{{ $order->order_number }}</td><td>{{ $order->customer_name }}<br>{{ $order->whatsapp }}</td><td>@foreach($order->items as $item){{ $item->product_name }} × {{ $item->quantity }}@if($item->variant_name) ({{ $item->variant_name }})@endif @if($item->toppings->isNotEmpty()) + {{ $item->toppings->pluck('topping_name')->join(', ') }}@endif<br>@endforeach</td><td>Rp {{ number_format($order->total_price,0,',','.') }}</td><td>{{ \App\Models\Order::STATUSES[$order->status] }}</td><td>{{ $order->created_at->format('d M Y') }}</td></tr>@endforeach</tbody></table>@endsection
